import 'package:flutter/material.dart';

abstract class ThemeColors {
  static const primary = Color(0xFF2D9CDB);
  static const primaryDark = Color(0xFF1D5F85);
  static const text = Color(0xFFFFFFFF);
}